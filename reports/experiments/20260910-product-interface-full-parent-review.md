# Repaired interface: independent1444 replay and next integration gate

Parent original66422 exited0: **1444 PASS in36.73s**, independently compiling and
running the exact frozen FW`ce25a80b37faee37ae841cea117a34977c0f1542` and
HDL`c4befff9a74bbf3743c3ce00eec2bbedbb09fd77`. Both worktrees were clean and both
commits unchanged before/after. This qualifies the additive primitive/actor
test scope, not the actual controller, vendor FFT, physical timing or receiver.

The new suite contributes596 tests. Original checked-reader328 and adapter520
remain848 separate tests, byte-unchanged from the earlier accepted source. HDL
changes from the archived prototype add exactly four files: two interface
variants, one fixture and one include; original runtime is unchanged/unconnected.
The original848 graph false negative remains a negative control, not a valid
whole-composition proof.

## Independently checked

Parent read the final runtime changes, full new Python suite/graph helper and
added fixture assertions. An independent reviewer also read the full source,
whole-body inverse patches and individual receipts before the parent replay.

- Exact exported READY governs private take; raw closed/X/Z offers remain
  observable with READY0. Descriptor, ordinal, LAST, lease and same-token GOOD
  checks remain. The508 original composed cases also run on the new primitive.
- State/GOOD/sticky-Q capacity is separate from exact current-safe guard ACK.
  Bad newly matured token verdict and raw offer faults block the current ACK,
  read or release before registered fault summaries arrive.
- Invalid qualified admission/completion capture is rejected and its detailed
  reason latched immediately; upstream summary observes reason Q one edge
  later. Real guard/product actor streams assert the required qualified-call
  and completion-before-final-product ordering. Forged pulses exercise their
  separately declared diagnostic-latency contract, not raw-fault equivalence.
- Healthy eight jobs retain512 consecutive core words each, zero holes,
  final-product→seal2/publication3, forward-completion→publication6,
  publication→ACK10, ACK→first-core2 and last-core→release1 clocks.
  Actor admission→release1555 is unchanged; it is not actual FFT service time.
- No added primitive register declarations or healthy pipeline stage. Logical
  bits remain760 plus one512×36 payload RAM, excluding guards/arithmetic and
  the future controller/epoch barrier; this is not mapped area.

The separate parent saved-run audit verifies all54 generated source manifests
(498 file entries), exact XML596/328/520 inventory, all eight healthy timing
receipts, and the base plus six mutated graphs. It loads only the previously
reviewed parser functions/constants, not the owner's graph verifier. The base
has3302 nodes including36 LS nodes and no cycle. Each independently restored
backedge has a cycle. This is continuous-net connectivity with procedural-state
cuts, not physical timing or arbitrary Boolean reachability proof. Whole-vector
connections remain conservative; no parser relaxation made the base pass.

The owner retained its earlier39PASS/1graphFAIL and587PASS/1expected-markerFAIL.
The latter mutant already failed at an earlier same-edge assertion; only its
expected marker was corrected. Missing-count and exit0/late-ERROR controls
demonstrate that a late failing assertion cannot hide behind an earlier PASS.
One Ruff import-order finding is documented, not reported as lint PASS.

## Evidence

Recovery:`product-interface-full-parent.Ujvjrt3p` under the persistent
`/home/mouse9911/gits/starlink-build-recovery-20260910.vHzUVnBz` parent.
Run command uses sanitized Python-B and these three test files, new cases/XML
paths, and `-q --tb=short -p no:cacheprovider`:

- `tests/starlink_oracle/test_product_sealed_interface.py`
- `tests/starlink_oracle/test_checked_product_read.py`
- `tests/starlink_oracle/test_product_sealed_adapter.py`

XML SHA`58b6fd4f0eecc7ade56bfb1fe100dbd7419099294d5e573853191dfce6c8029d`;
parent verifier`6bf27a8d535951f7daa5030cce1301da95db41b790b10220adeee15265783af0`;
audit JSON`d3f5a41fbafde44507bc9774d7a167eb7dab68f28713f8a81d983e3ae94558ad`.
Archive`20260910-product-interface-full-parent.tgz`:4427535 bytes,
SHA`1918163d39f6b0a3d890ab0008818e41f2e8caad4583c2e8f08ef7538af17627`;
tar comparison exited0. It retains actual compiled sources/netlists, all test
receipts/logs, XML and independent audit. Only pytest convenience symlinks
ending`current` are excluded. Full replay helpers/inverse patches are in the
exact FW/HDL commits above.

## Next gate, not deployment approval

Source-specific real P1 controller integration design is now the next task:
independently bind forward-admitted lease and expected job metadata to checked
head; use exact ACK receipt and fault-qualified emitted input-start; add the
fresh common-epoch/paused-slow purge barrier. Disabled observational diagnostic
ports may report hostile inputs and must not be ORed into the legacy path;
only ownership/transfer outputs are promised inert. The fixture's disabled
input identity check is not a production exemption.

No top edit/vendor/clock/PPU/radio/main change was authorized by this replay.
Keep this alternative separate from retained-output scheduling until each is
qualified. Complete receiver timing, actual60 calibration, canonical15 coarse
plus native fine, independent2.5MS/s IIO,120ms/300s scanner, and `.18` then
Ethernet-only `.17` deployment remain required. The last physical route is FAIL.
