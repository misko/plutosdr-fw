# Bank-local identity: actual-qualified synthesis binding

The unbound recipe remains byte-identical to the independent80-test source
snapshot. The new binder exports a separate source tree, changing only two
literal blocks: accepted actual provenance in admission.py, and the exact actual
manifest in the synthesis Tcl. Both changes have strict complete-source
inverses; the other22 source assets remain identical. No RTL, clocks, route,
FFT factory or directives change. The original derive.py and policy report in
the exported tree describe its unbound ancestry; binding.json records the two
subsequent changes. Do not treat the unbound ancestry report as current admission.

The binder has no user-supplied evidence override. It pins the real original
bank-local actual owner, command, stdout, terminal receipts, result and CLI. It
checks the live actual admission and complete result read-only during export.
No vendor invocation occurs in binding or physical preparation.

## Independent gates

- Original80 preparation-policy tests PASS,24 source assets unchanged.
- New19 binding tests PASS: real read-only admission, two complete inverses,
  wrong/old manifest/run/owner/result/CLI rejections, incomplete provenance,
  changed binding text and output containment/no-overwrite controls.
- Previous actual preparation64 and bank offline59 passed independently.
- Root actual18768 finished0 in56.82s, with original/copied source checks and
  complete result verifier0. Actual77953 CSV rows and all prior result fields
  match the offered-summary reference. New full guard checks total3230028;
  preflight and selected-input premise checks each179446.

Actual bundle SHA256:
`e83b111be699f6391a0ac03f961846d8798d2ee109fcc69f8ce175e4ec7b25f5`.
Actual result SHA256:
`e8a984ae215a494f9d1d5dd210289c6643be0ef94afce93bdd4f310b51a3285a`.

The real copier produces167 inventory entries (plus SHA256SUMS), digest
`0741084843df9991a536ab6894d94ac56eecd1b3c69152ec9ad7b790ae014d9b`.
Bound Tcl SHA256:
`40493ffe85fe34a6196f3f3c8b98a33ba59756abc8ce426c525fb6e79fbfcb9a`.
Root source review covers the full copier/admission/derivation/Tcl/observer,
all519 original policy-test lines and new binder/tests. The root synthesis
owner has four literal mapping groups and a complete inverse to the previously
reviewed source. Source validation precedes launch and follows terminal exit.

Evidence under `/home/mouse9911/gits/starlink-build-recovery-20260910.vHzUVnBz`:

- `bank-physical-prep-parent.uhVRRMbq`: independent80, source snapshot, new19 JUnit.
- `bank-identity-physical-bound-source-v1`: bound source and binding.json.
- `bank-identity-synthesis-prepared-parent-v1`: immutable167-input package.
- `bank-identity-synth-parent.UqxKeHuT`: gate, owner derivation and physical run.
- `bank-identity-actual-parent.eL66fuq9`: original actual run and independent audit.

Synthesis is a diagnostic isolated-block build, not full receiver timing closure.
The last routed design still fails at-2.504ns. Consult the original process's
execution.json for the new synthesis outcome; this preparation report does not
assert a physical result. No radio, PPU or main-branch operation occurred.
