# Destination absorption: exact contextual algebra, unrestricted scalar rejected

Offline source-bound result only. Original handle54525 terminated0: **15 tests
passed in2.54s** (owner wall2.69s). No runtime, factory, observer, constraints,
vendor simulation, synthesis, route or radio was changed/run. No publication or
RTL integration is inferred. The three original source files are byte-pinned
to HDL `b013cb91ffc8038e39726109ebfe5ee5afe032ed`'s immutable synthesis inputs.

## Frozen expression and result

Let R be actual fast_running, B the literal output-mailbox readiness
`R && !input_fault && (request_toggle == acknowledge_sync[1])`, and K the case-exact
knownness of the retained owner's private reserved Q bit. Let S be the full
original common aggregate with only a PARALLEL preflight destination term
using `next_inverse ? B : product_bank_ready`.

The selected experimental equation, frozen before the first simulation, is:

```verilog
contextual_common = (R === 1'b1 && K) ? S : original_common;
```

It is case-exact across the finite decomposed proof, including unknown phase,
preparing, selected-summary option, mailbox controls, owner Q values and reset.
All six original preflight reason bits remain literal and unchanged. Both full
original/offered external OR expressions are copied by name-only substitution
with whole-statement inverses. Their independent retained current-fault and
all eight sticky reason bits are essential; no destination reason was deleted.

Unrestricted raw S is **rejected**, not reported as qualified. Its first actual
counterexample is R1/B1/request0/ACK0/reservedX/published0/expected_request0,
lease00/held00/reasons00/current0, phase1/preparing1. The original preflight vector
is `0x0000`; original aggregateX becomes raw scalar0. K is therefore an explicit
fallback obligation, not an omitted reachability assumption. The same issue
also occurs with reservedZ;72 aggregate contexts differ in the exercised matrix.

A separately frozen analytical tightening B (`B && K` as inverse destination)
changed384 originalX contexts to known fault1 and never weakened or changed an
original known0/1 result. This is NOT the selected proposal: exact fallback C is
preferred. The owner currently has no K output; any future observational clone
must retain all old state/release/reason equations and be reviewed independently.
X tests in simulation are not a claim of physical unknown-detection hardware.

## Exact proof scope

| Receipt | Count |
| --- | ---: |
| Real source owner input/state combinations | 98,304 |
| Complete consumed owner-output signatures | 110 |
| Aggregate contexts checked | 333,056 |
| Raw scalar equal in known-reserved contexts | 236,288 |
| Original reset R0/X/Z fallback contexts | 124,416 |
| Raw scalar mismatches retained | 72 |
| Separate tightening X→1 contexts | 384 |
| Exactly preserved clean contexts | 5,153 |
| Independent fault-root/retained-reason/option cases | 13,824 |
| Conditional six-cause reduction lemma cases | 2,916 |

Seven owner scalar controls/state use all four values; both case-inequality
lease classes and all three reason-reduction values are enumerated. All256
two-bit lease pairs and all65,536 four-state eight-bit reason patterns are
additionally executed. Every phase/preparing/product-ready/summary-option value
is covered per consumed signature. The other five preflight causes reduce to
0/1/X; the exhaustive six-cause conditional lemma proves this reduction even
under an unknown gate. Logical OR associativity then covers simultaneous
independent roots, each of which is also directly driven in all four states.
This is a finite combinational decomposition, not an exhaustive sequential
controller proof or merely a set of legal-job simulations.

The actual mailbox readiness statement is compiled literally. The entire actual
retained owner is compiled without edits, and arbitrary internal Q is assigned
without clocking. Consequently this proves neither actual reader release/ACK
events nor inductive reset/admission reachability. Event controls are tied idle;
their prior sticky effects are included by all reason-Q patterns. No guarantee
about an unmodeled producer/consumer clock sequence is asserted.

Executed negative controls all stop nonzero at the exact contextual-equality
assertion: missing independent retained current fault (old1/new0), missing
retained reasons (1/0), missing K fallback (X/0), and wrong constant-zero original
fallback (1/0). Source-pin changes are rejected for each of the three sources.
Six receipt negatives reject bare/missing/duplicate/wrong-count/wrong-witness/
late-error logs; parser-only synthetic rows are not simulation evidence.

## Preserved chronology and repeat

All attempts remain under
`/home/mouse9911/gits/starlink-build-recovery-20260910.vHzUVnBz`:

- `retained-destination-algebra-v1.gm7dNrvA`: terminal1,4PASS/1 compile failure.
  An Icarus mixed initialized/uninitialized wire declaration was rejected.
  Candidate expressions were already frozen; only splitting that declaration
  repaired the testbench syntax. No algebra result is claimed from v1.
- `retained-destination-algebra-v2.cEp2yJFJ`: original19977 terminal0,5PASS1.86s.
  Full matrix and original rawX→0 counterexample retained.
- `retained-destination-algebra-v3.uzHAAKe0`: original54525 terminal0,15PASS2.54s.
  Adds literal name-only external copies/inverses and executed mutants/strict
  receipts without altering A/B/C expressions. All original outcomes retained.

Repeat from `/tmp/starlink-rom-prefetch.j829ht/fw`, using a fresh non-/tmp
`--basetemp`/JUnit/log directory and preserving the original returned handle:

```sh
env -u PYTHONHOME -u PYTHONPATH -u LD_LIBRARY_PATH -u PYTHONOPTIMIZE \
  PYTHONDONTWRITEBYTECODE=1 /home/mouse9911/gits/pluto-plus-utils/.venv/bin/python -B \
  -m pytest -q -p no:cacheprovider tests/test_retained_destination_algebra.py
```

Final source pins:

- helper `tests/starlink_oracle/retained_destination_algebra.py`:
  `62d49494e09ff3f19cbc09265ea9cd42de60a5a322bd18b3b2f2f872962eeffe`
- bench `tests/starlink_oracle/tb_retained_destination_algebra.sv`:
  `1d7960ab536a0f94ca7de438d6d6a6707e2ddb1f1520e471ce17b43dc8cd688a`
- tests `tests/test_retained_destination_algebra.py`:
  `afe3d48e83864685e0bb0b9c841afee247975ef2466ac1f5e006a377aae036f5`
- pre-evaluation contract `docs/starlink-retained-destination-algebra-contract-20260910.md`:
  `6e51bcf2aadbb225fa7f1dfb8192b4db789f9b84326959416adca750ab5e1d50`

No new register is suggested by the algebra; mapped logic, latency, fanout and
timing are unmeasured. The original detailed vector and explicit fallback still
exist. A synthesis tool's treatment of that fallback cannot be assumed. This
does not address held-phase input-guard identity, completion-receipt D, or all
reset fanout. Write-side delayed commit and cleanup-mask ideas remain unimplemented.
Any actual gate must reuse the retained seven-context service/numerical/reset
campaign, including its own frozen parser's ready-profile absolute5215 cap and
exact prior event/result equality (observed nominal3645/parked4912). Correction:
the earlier report prose incorrectly characterized5215 as only the checked lane;
retained_output_actual_result.py line108 also enforces it. The frozen v3 report
and all proof/source artifacts remain preserved; no parser or gate ever changed.
