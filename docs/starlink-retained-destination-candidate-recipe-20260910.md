# Pre-evaluation contextual destination candidate recipe

Bounded offline candidate, default off. Three additive RTL files select uniquely
named wrapper/top/owner modules; no canonical RTL or frozen15 algebra file changes.
The selected C expression is the previously proved exact contextual fallback.
The original six preflight reason bits, full current/sticky roots, all owner17
state bits and every owner current/release/event/reason equation remain literal.
Owner adds only a read-only reserved-knownness output; runtime uses that port,
never a hierarchical private-state reference. X/Z is simulation semantics, not
a physical unknown detector. No new FF or cycles are intended; mapping unmeasured.

Before simulation require exact whole-file inverses for owner/top/wrapper using
the fixed before/after pairs in `retained_destination_inverse.json`. Also require
the immutable original119-file manifest SHA b5d11256... and116 source closure;
all26 compiled source paths and full original63722-byte bench come from its
reviewed helper, not reconstructed actor stimulus. Three compiled paths change;
the old owner is added solely as an independent same-input shadow.

Planned gates: default0/enabled1 complete seven-context offline scripted campaign
with all old numerical/status/frame/service/reset/owner/ROM/arithmetic observers
unchanged; full CSV and entire inherited parsed result match the original.
Additional common predicate and old owner17-state/all-output checks occur preedge
and+1ps. This is OFFLINE_SCRIPT_NOT_FFT, never actual vendor qualification.
Natural inverse admissions/publications/releases remain19/19/17 (two reset-discard
owners remain discarded). No counted reset-as-release or timing/gate adjustment.
The original parser's line108 ready-profile absolute cap5215 is authoritative
and unchanged, together with exact prior event/result equality and observed
normal3645/parked4912 service. Earlier analysis prose distinguishing5215 as only
the checked lane was overbroad; the frozen retained parser also includes it.

Separate real old/new owner test: all262144 combinations of eight four-state
public inputs at four naturally reached phases, pre/post all state/output
comparison, explicit current common fault0/1/X/Z at real ready-high ACK, reset
and private Q X/Z observational witnesses. Forbidden caller combinations are
intentional boundary tests, not top reachability assertions.

Require executed missing current/reason/fallback/knownness controls, whole-source
inverse corruption negatives, invalid option/binding failures and strict terminal
inventory. Preserve every original failed attempt. No vendor/synthesis/route,
bank-local identity union, cleanup/delayed-write proposal or radio action.
