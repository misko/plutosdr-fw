# Pre-evaluation actual FFT recipe: contextual destination only

Keep the original offered-summary116 source authority, complete seven-context
bench, numerical vectors, frame/status/reset/service references and parser.
Use the three exact independently tested destination runtime files; no bank-local
identity union. The entire existing composed bench (default option1) is retained,
including the original owner shadow, exact common predicate comparison and all
17 owner-state bits/public outputs. Append only an explicit wrapper/top option
readback receipt. No new runtime state, clock, driver, skipped epoch or latency.

Prepare a standalone bundle containing the old116 sources plus an additive source
overlay. It changes exactly three compiled runtime paths and adds the unchanged
old owner module solely for the observer (27 runtime/reference paths plus bench).
All eight vectors stay unchanged. The benchmark inverses in the existing helper
restore the complete original bench; an additional inverse removes only the new
readback observer. No numerical parser replacement or shortened actor campaign.

The fixed original source bundle is an explicit read-only dependency of the
candidate helper and verifier. Its manifest/hash/file inventory is checked before
every helper child invocation. Children use the frozen source namespace and a
sanitized, unoptimized interpreter. The generated bundle also carries all116
original sources; compiled HDL comes only from the copied bundle. Live/copy
integrity covers the full additive overlay, original authority and generated
bench/profile; external manifest digest and one-shot output are mandatory.

Before vendor execution, require offline complete77953-row CSV equality and exact
entire prior result, plus source/manifest/bench/profile/option/receipt negatives,
unchanged old CLI/runner failure cleanup and language-selection behavior. Original
ready-profile cap5215, event timing, observed3645/4912 services,40 admissions,
38 completions,19 pairs and17 real releases remain unchanged.

Run one root-owned Vivado2022.2 behavioral campaign only after the preparation
tests pass. Accept actual results only after original complete verifier plus
unique exact option and composed17/19/19 release/admission/publication receipts.
Preserve original/copy source checks, generated FFT pin and original process
terminal outcome on success and failure. Actual success is not synthesis, routed
timing, CDC, continuous60MS/s or RF/deployment qualification.

After the initial81 tests passed, tighten the additive receipt gate before any
vendor evaluation: pre/post owner observations must equal the unchanged last
context's final cycle, rather than merely exceed1000. Add negative counts2000
for each side. Also verify the complete prior CLI inverse on every source check,
not only by its recorded generation. These are stronger preparation checks;
runtime, witness, stimulus and original acceptance are unchanged.
