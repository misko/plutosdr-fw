# Retained control candidate: actual preparation

Preparation is complete; no vendor execution occurred in this lane. The parent
must separately authorize and own the actual invocation. The previous physical
result remains timing-failing and CDC-unqualified.

Tested source: FW `77ffe5d82c2dc80a93cc6855da9c816aa56d5d53`, HDL
`3ecfd9f33a8b9cfe44dddc731c768325871f7936`. The recipe was committed first at
FW `2ab2d2678`, before scripted evaluation. Its SHA-256 is
`302d34f16fe6d2dc6cb2ee9a7d64ac864a18ff9dbe98d6050544d86493259b25`.

## Frozen package

Recovery root: `/home/mouse9911/gits/starlink-build-recovery-20260910.vHzUVnBz`.

Bundle: `retained-control-actual-prelaunch-v1` under that root.

Manifest SHA-256:
`7a9b32f10241d22c3f5a6d3967ca9841e2646bb94d714f4935c05f5e8d03635e`.

97-source signature:
`b52ba499ffbec7b4596afaba5b180d8145381ea56a4af60ab4cce30c79631908`.
There are 100 file receipts plus the manifest. All original 80 source bytes
retain signature `9c7996e6d61a559ddfe99543b80c62063b3a5fed2cd43f7aa7a43b1f664af4f7`.
Only four compiled runtime paths select the already frozen candidate copies;
none of those runtime bytes changed. All eight vectors, the original factory,
26 compiled modules, and original seven-context orchestration remain unchanged.

The new CLI is a one-import literal inverse of the qualified CLI. The new Tcl
runner is a three-string inverse: its own path, CLI path and terminal label.
Thus the resolved Python pin, subprocess-only environment sanitization, two
threads, exact SystemVerilog file selection, original generated FFT factory,
before/after original and copied-source checks, generated-IP checks, and original
failure preservation are retained. No new timing options or generics are added.

Runner SHA-256:
`7d49b8e3751e451223dcea24f5f4594394f630d77c55b9b0c9272811c8b7cad8`.

## Evidence

| Attempt | Original handle / terminal | Result | Recovery directory |
|---|---|---|---|
| First new harness | 76829 / 0 | 48 passed, 16.59 s | `retained-control-actual-v1.1sXuqUlF` |
| Bundle admission | 21088 / 0 | 25 passed, 1.03 s | `retained-control-bundle-v1.y6C6ipNZ` |
| Final combined scope | 79851 / 0 | 73 passed, 17.49 s | `retained-control-prelaunch-tests-v1.Lz40I0kG` |

The final 97 source hashes match before and after. Logs, XML, generated benches,
scripted CSVs, and deliberate parser/Tcl-stub failure specimens remain in place.
The final command was sanitized Python `-B -m pytest -q -p no:cacheprovider`
with exactly `tests/test_starlink_retained_control_actual.py` and
`tests/test_starlink_retained_control_actual_bundle.py`, a unique non-`/tmp`
`TMPDIR`/`--basetemp`, and explicit `--junitxml`/log paths.

The complete 77,953-row candidate CSV byte-matches a fresh unchanged v5 scripted
run, SHA `07321b026a637e5922c56a84a955e58549056337198c952a9d73b1245cb4efaa`.
These are scripted results, not vendor execution evidence.

All three original guard references receive the literal **original unrestricted**
completed-input expression. The complete 136-bit forward shadow and both
unconditional old guard/state shadows pass, including invalid metadata. No
comparison was masked. The new witness records 40 accepted descriptors, 38 final
input edges, 49,419 pre-edge and 49,419 settled post-edge closed-predicate checks,
and 19,456 public-return uses. Owner totals are forward 21 admitted / 19 real ACK /
34,719 held checks; inverse 19 / 17 / 60,660. These measured comparison counts are
not substituted for the predeclared acceptance bounds.

The result verifier first runs the unchanged original parser on the complete,
unfiltered log and CSV, including all frame/reset/configuration and row-time
joins. It then requires 44 new `RCAND_` records and exact admission/identity joins.
It never migrates legacy logs or supplies missing receipts. Missing/duplicate
markers, every new receipt field, source/profile/options, mutated snapshots,
canonical numeric aliases, wrong runner/Python/version and complete-log fatal
markers are rejected by the bounded tests. Executed Tcl stubs preserve the
original create/launch failure, both source checks, and generated-IP receipts;
a changed copied source fails its after-check. They do not invoke vendor tools.

## Parent-owned actual invocation

The runner takes four arguments: absolute frozen bundle, exact resolved Python,
external manifest digest, and an absent output directory. Use the same previously
reviewed one-shot owner: separate absent owner/output/log/journal paths, 900-second
process-group cap, no retries, and final original/copy/IP integrity even on error.
The parent must check all target paths before launch. No output path is reserved
or created by this report.

Vendor executable/environment: `env LD_LIBRARY_PATH=/opt/Xilinx/Vivado/2022.2/lib/lnx64.o/SuSE /opt/Xilinx/Vivado/2022.2/bin/vivado`.
Put `-log` and `-journal` before `-tclargs`; use `-mode batch -source` with the
bundle's `source_snapshot/hdl/library/starlink_pss_acquisition/retained_control_actual/simulate_retained_control_actual.tcl`.
Python is `/home/mouse9911/.local/share/uv/python/cpython-3.11.16-linux-x86_64-gnu/bin/python3.11`,
SHA `2874a0b9344d06b7767aebb1e6e25a759ffcbdb544e99400ecc74dc6092d1174`.
The runner sanitizes only the Python child environment; the parent Vivado loader
must retain its SuSE library setting.

No continuous 15 MS/s/native 60 MS/s, lower-clock, causal acquisition, RF,
physical closure or production promotion claim follows. No vendor execution,
runtime edit, main merge, radio/PPU action or new preparation push was performed
in this lane. The earlier 39-test candidate/algebra package was separately
published on DNM and independently verified directly from Git objects.
