# Healthy60 paired harness: offline prelaunch only

Status: 311 additive tests passed in 6.15 s on `gauss` (Python3.11.16,
Icarus12.0). No combined service, vendor FFT, Vivado project, physical build,
radio or host-driver operation has run in this stage. The full composition was
compiled with an explicitly inert, fail-fast FFT interface and never executed.

Tested FW `aac863cea50cffd99b90f516286751c38e15c8b9`, HDL
`88195cd9029a0c66f642fe21045ff70053fc46de`. These commits add tests only.
The two StageA60 runtime files remain byte-identical to approved `f16dc564`;
all original30 sources, native60 arithmetic and 69 numerical files are unchanged.
Public PSMA1.8 still depends on experimental, unpromoted StageA30/60 admission;
host/kernel ABI, receiver profiles and deployment support remain pending.

## Frozen inputs and exact acceptance

`build/high-rate60-harness-prelaunch-v1/bundle.json` SHA256:
`b5f7d48a96217691d7a034634d3bdc7006e489066e571d93b00ce2ed5f741714`.
Its 108-source signature is
`26f33d8727828eabd56d24466984168be3bdc52fc5e0166ba8ffe94f0e89b8e8`.
The bundle has 250 input files (6,300,980 bytes excluding its manifest) and
is byte-identical to the final test-generated bundle. All live source hashes
match the frozen copies. The snapshot CLI independently verifies the bundle.

The pre-evaluation recipe remains commit `dd266118`, source SHA256
`1b24f01680b09313dcfd827cce45a8771d86892adef6773f0457c7b5fefba385`.
The original numerical manifest remains
`6de2f3645459f8649d8fee127e479991fb1cc55b75001a37c763223e716b4dfa`.
No numerical oracle is regenerated by the new preparation helper: it copies
all69 byte-for-byte and adds only two raw-source memory files containing the
two declared prime words followed by all16,423 original words/indexes. No tail.

| Evidence required by future actual run | Frozen acceptance |
| --- | --- |
| Public composition | PSMA1.8, caps0x7ff, source60, DDC0x020f0403, delay21, Eh1073765335, all8 kernel-identity words; public bank/STOP/pilot selectors |
| Original source | `[34359735211,34359751634)`,16,423 samples; separate disabled primes `[34359735209,34359735211)` with CI16 words1,2 |
| Startup | 3,111 original raw samples →1,549 first-stage30 outputs →768 canonical; then2,000 control-clock drain/pause before coarse enable |
| Continuous segment | All13,312 remaining original samples `[34359738322,34359751634)` on successive true60 source edges, through capture and source-off |
| Coarse map | Exactly894 admitted scores,447 map words, full retained-map read/release; every visible score, including bounded tail through1,341, compared |
| FFT evidence | Every visible512-word block prefix, input packing, forward/product/inverse, both BFP exponents, index/last, energies and69-bit normalization;1,024..3,584 words/stage |
| Native | Original264 coefficients,520 capture,257 raw tuples,241 qualified, two26-word public reads; request60000520/generation60000001; visit60000052 is fixture context, not native packet field |
| Native limits | Actual command index34359738560..34359738720, lead1535..1695,≤256 control cycles; publication and full257 drain≤84,000 after capture; release≤88,000; tuple hold≤16; AXI≤24;≤140 native readout transactions |
| Pilot | All512 CI16 words/2,048 bytes, each newest index, every visible mixer/halfband/FIR prefix,90 initial unsupported results, coherent26-word PIL1 snapshot and visit60000052 |
| Lifetime | Map retained through independent native result release; source-off may precede both releases; full257 drain required even after publication;256 final no-stale control cycles after all reads/releases |

Source time0 starts low, waits2.1ns, then toggles each quantized8.333333ns:
first rise10.433333ns, first fall18.766666ns. FFT clock starts low, waits1.3ns,
then toggles each2.857143ns: first rise4.157143ns, first fall7.014286ns.
Control period is10ns. The oscillator observer checks first edges, every half
period and full period; result verification reconciles edge totals with the
final quiet endpoint. These are ideal digital clocks, not a generated MMCM.

The startup pause is outside the continuous13,312-sample segment. The complete
16,423 original samples are NOT claimed wall-clock uninterrupted. The two x2
ledgers each use14-history inputs and six pre-edge observation slots (issue to
registered output is five control cycles). They observe an already-promised
old output before canceling later pipeline work when disabled. They compare
every visible stage word/index/mixer and actual cumulative accepted/emitted
counts. Expected enable comes from separately checked programmed controls,
real STOP ACK/public ticket receipts and the matched512th pilot admission,
not from trusting the DUT's internal enable as its own oracle.

The raw-enabled minimum14,475 and canonical minimum3,609 are support-only
bounds. They do not replace the exact per-edge ledger or permit claiming all
4,096 canonical outputs/seven FFT jobs survive pilot auto-stop. Native command
and capture cycle receipts are additionally tied to the continuous raw-clock
coordinates. Actual FFT-clock input handshakes during actual native capture,
native compute concurrent with coarse/pilot activity, and compute after STOP
must all be observed. A static elapsed-time overlap does not satisfy these gates.

## Implementation and policy coverage

New HDL entry `simulate_high_rate60_bank_native_paired.tcl` and
`tb_starlink_pss_60_bank_native_paired.sv` use only public rate/bank/STOP admission;
the seven defparams override reduced map geometry only. The new source, FFT
and clock includes are independent of unchanged30 benches. Existing native60
budget/readback and three-port direct AXI includes are reused unchanged.

`high_rate60_harness.py` prepares/validates the fixed cohort;
`high_rate60_harness_result.py` validates complete results. Its native component
is checked by strict complete-function AST equality against the original
verifier after eight enumerated outer-context edits. No inner native bound,
tuple/source check, ordering, message or expression may drift. Outer paired
receipts replace the standalone terminal without falsely manufacturing one.
The expected paired marker inventory is explicit:512 pilot-word lines plus
nine required singleton markers, total521. Unknown/duplicate/missing markers,
mixed-case failure text, altered raw/capture/hold/packet/PIL1 data, wrong clocks,
source coordinates, deadlines, aliases and identity mutations reject.

`high_rate60_bundle.py` hard-pins the91 earlier native sources, substitutes only
the two reviewed StageA60 bodies, freezes the original76 plus imported and
additive dependencies, and ties each source digest to actual snapshot bytes.
It rejects source/output symlink parents, overwrite, alternate recipes/types,
changed numerics, source signature lies and self-consistent forged pin tables.
Preparation checks live sources before/after copying; the runner verifies the
external bundle hash before admission and frozen inputs after success/failure.
Original project/simulation/cleanup errors are preserved; no terminal receipt
is written after failure. Tcl policy tests use explicitly labeled create/launch
stubs, never a vendor tool. Python environment sanitization is subprocess-only.

The complete compile-only run retained three diagnostic lines: the inert
interface inherits the top's timescale; unchanged bank RTL leaves optional
`idle_mailbox_fault_now` unconnected. Its frozen `USE_IDLE_MAILBOX_FAULT=0`
selects the historical mailbox input. This is not a warning-free claim.
The separate200ns clock/X/Z-health unit instantiates no native/FFT/DUT modules.

## Tests and preserved attempts

Exact final scope (311 tests):

```sh
env -u PYTHONHOME -u PYTHONPATH -u LD_LIBRARY_PATH \
  /home/mouse9911/gits/pluto-plus-utils/.venv/bin/python -B -m pytest \
  tests/test_starlink_high_rate60_harness.py \
  tests/test_starlink_high_rate60_harness_result.py \
  tests/test_starlink_high_rate60_bundle.py -q \
  --basetemp=/tmp/starlink-highrate60-harness-tests-v4 \
  --junitxml=build/high-rate60-harness-offline-v4/junit.xml
```

Use a new unique basetemp/log directory for an independent repeat. Final
original handle62397 exited0; raw outputs remain under those exact paths.
Ruff passed. No unchanged660 regression was replayed.

Earlier attempts remain intact: v1 original92660:165PASS/one policy-test failure
(repeated energy literal required a context-specific source check); v2 original
18543:305PASS/five mutation-test failures (removing only newline did not alter
splitlines data; changed to remove actual payload); v3 original30368:311PASS.
No runtime, golden or acceptance-budget change was made to resolve these.
Final v4 adds known-zero/known-protocol guards to the new evidence only.

Portable preparation inputs, report, collector identity, all four pytest logs/
JUnit files and final compile/logic-unit receipts are in
`reports/experiments/20260910-high-rate60-harness-prelaunch-v1.tgz`; its adjacent
JSON lists every safe regular member and archive SHA. Generated parser-only
specimens/stub-project trees remain at the full raw basetemps, not duplicated
as supposed actual evidence. Archive assembly is not part of the311-test claim.

## Future launch remains separately gated

The new runner requires exactly NEW_RUN, FROZEN_BUNDLE, explicit Python and the
external reviewed SHA. Use Vivado2022.2 with the parent environment
`LD_LIBRARY_PATH=/opt/Xilinx/Vivado/2022.2/lib/lnx64.o/SuSE`; place log/journal
options before `-tclargs`. It fixes `general.maxThreads=2` and reuses the original
18-bit real-FFT IP helper unchanged. There is no actual launch authorization in
this report. Success would qualify only this bounded healthy known-center
digital composition, not causal acquisition, RF/timing accuracy, shared-host
AXI service, DMA/IIO, physical60 capacity, full-production maps or deployment.
