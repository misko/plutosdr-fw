# Offered-summary seven-context actual preparation

Preparation passed 67 offline tests. No actual FFT, synthesis, route, radio,
source-copy admission to a real run, or clock/constraint change was performed.
All vendor execution remains parent-owned and separately authorized.

## Frozen source

Recipe committed before evaluation: FW `93b9210a8`, machine recipe SHA
`4831bf1a03f306075aa02483df6497e6e72ebf78b1467415e8e364a3b0e58529`.
Tested source commits are FW `d0a1693fcb72a78b96dddb38141a385672c1459c` and HDL
`b013cb91ffc8038e39726109ebfe5ee5afe032ed`. Runtime remains the exact offered
candidate from HDL `48b82653d`, already offline51-qualified; this preparation
adds only a read-only witness and source-specific helper/bundle/runner/tests.

Prepared directory:
`/home/mouse9911/gits/starlink-build-recovery-20260910.vHzUVnBz/retained-summary-actual-prelaunch-v1`

- External manifest SHA:
  `b5d112562b7db31164dc4a6ff92404de8e7d7d5d96b1c1b23e1a7dbac9d2c368`.
- 116-source signature:
  `cf37ea269f9c220a3cfeea1761757657bccc5faff62aade4dec1d87a7d0694fb`.
- Complete inventory: 119 files plus manifest, 120 regular files.
- Original qualified97 signature remains
  `b52ba499ffbec7b4596afaba5b180d8145381ea56a4af60ab4cce30c79631908`.
- Frozen runner SHA:
  `177a1961cdbeca4dc0f68a8994e31fc5b4145690c2f80fc26b916b8fba93f8d1`.
- Frozen standalone CLI SHA:
  `e0d91e6c5942e6a6d6d2a358e6e9f0c485c7e6d7ea3078eba2874f25f3107875`.

## Exact scope and inverses

The 26 compiled names select only four new runtime paths; all other modules,
eight memory files, real FFT factory and numerical policies are unchanged.
The new bench strictly reverses two additive edits to the complete accepted
control-candidate bench: `INPUT_OFFER_FAULT_SUMMARY=1` and one read-only witness.
The original private/closed options, all three full original fault-reference
bindings and all unconditional output/state shadows remain exact.

The runner has a literal three-name inverse to the accepted control runner;
CLI has a single import inverse. Exact SystemVerilog source selection, part,
Vivado2022.2, two-thread limit, known resolved Python binary/hash, subprocess-only
environment sanitization and original failure-preserving after-integrity/IP
receipts are inherited unchanged. Output containment, symlink, no-overwrite,
external digest and full snapshot/source joins remain mandatory.

The original complete result parser processes every log and CSV first. Only
then are exactly two new markers admitted: option forwarding through wrapper,
top, cutover and both owners, plus pre/post premise counters. New counter inputs
are joined to physical inputF/inputI rows, including aborted prefixes; all 38
complete-input ends remain required. Missing evidence is never migrated.

## Executed offline gate

Original handle75338 terminal0: **67 PASS in 19.54s**, retained in
`retained-summary-actual-first.jtRe2yOy` under the recovery root. Exact files:

```sh
env -u PYTHONHOME -u PYTHONPATH -u LD_LIBRARY_PATH -u PYTHONOPTIMIZE \
  TMPDIR=UNIQUE_RECOVERY /home/mouse9911/gits/pluto-plus-utils/.venv/bin/python -B \
  -m pytest -q -p no:cacheprovider \
  tests/test_starlink_retained_summary_actual.py \
  tests/test_starlink_retained_summary_actual_bundle.py \
  --basetemp=UNIQUE_RECOVERY/pytest --junitxml=UNIQUE_RECOVERY/results.xml
```

The complete new scripted CSV is byte-identical to the unchanged control
script. Its entire parsed result is equal after removing only `offer_summary`.
All 77,953 rows, 40 frame records, 38 complete jobs, two aborted F prefixes,
19 pairs and original44 candidate receipts passed. The new script observed
89,465 pre-edge and 89,474 settled post-edge checks, all known-zero input fault;
9,857 forward and 9,728 inverse physical input beats, 38 ends and 357,878 routed
checks. These are scripted measurements, not vendor performance predictions.

Tests also execute a wrong-option readback failure, complete source/bench
inverses, every new receipt-field mutant, missing/duplicate old/new markers,
extra fields/markers, wrong input counts, corrupted numerical data and appended
`FATAL_ERROR`. Bundle tests include the literal old Tcl create/launch failure
stubs, launch-time source mutation with failed after-integrity, incorrect runner,
version/interpreter, path/overwrite/source/profile/identity mutations, and frozen
base-Python CLI verification from `/`. All simulated/stub outcomes are explicitly
classified; no fake vendor success receipt is produced.

## One-shot invocation contract, not launch permission

Parent chooses an absent exclusive owner directory, run output and external
Vivado log/journal. Invoke only the bundle's frozen runner, with log/journal
options before `-tclargs`, and arguments in exactly this order:

```text
BUNDLE  RESOLVED_PYTHON  EXTERNAL_MANIFEST_SHA  NEW_OUTPUT
```

Resolved interpreter:
`/home/mouse9911/.local/share/uv/python/cpython-3.11.16-linux-x86_64-gnu/bin/python3.11`.
Vendor parent environment must provide only the established
`LD_LIBRARY_PATH=/opt/Xilinx/Vivado/2022.2/lib/lnx64.o/SuSE`; independent Python
children clear PYTHONHOME/PYTHONPATH/PYTHONOPTIMIZE/LD_LIBRARY_PATH. The original
process is owned to terminal with no retry; both source copies, original bundle
and generated IP must remain unchanged even on failure. Exit0 alone is not a
pass: original terminal, strict old/new result verifiers and full integrity
receipts are required. No lower-clock, continuous15/native60, causal, RF, or
physical claim follows from this preparation or the proposed seven-context run.
