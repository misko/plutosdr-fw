# Offered fault-summary actual preparation recipe

This additive preparation uses the accepted control candidate actual campaign
(original61512), unchanged seven contexts H0/H1/H2/H4/H5/R1/R2, 40 admissions,
38 complete jobs, two aborted F input prefixes, all original numerical rows,
causal frame/reset/configuration joins and service limits. No vendor execution
is authorized by this document or preparation tool.

Only four compiled runtime paths select the frozen offered-summary copies.
`INPUT_OFFER_FAULT_SUMMARY=1` is added alongside the existing private/closed
options. The bench is a strict additive inverse to the full accepted control
bench. All three old guard references retain the original full predicate; no
mask or substituted summary is allowed into a reference.

Add two read-only receipts: one checks exact wrapper/top/cutover/two-owner flag
forwarding; one totals pre-edge and settled post-edge premise/equality checks.
Every enabled edge uses the actual input fault, original common expression,
offered common expression, physical certified strobes and exact routed masks.
Known fault0/1 must retain case equality; X/Z direct input fault must produce
known-one summary, never healthy authority. The seven healthy/reset contexts
expect no enabled nonzero/unknown input fault; adversarial behavior is qualified
only by the separate offline51 and parent algebra/graph gates.

InputF/inputI accepted counters are joined independently to the complete
original CSV, including aborted prefixes. All 38 full-input ends are required.
At least 1,000 observations per pre/post region prevent an empty proof. The
original parser runs on the entire unchanged log/CSV before the two additional
markers are validated. Missing/malformed proof is rejected, never synthesized.

Before any actual invocation the new scripted CSV must byte-match the old
control campaign and its full parsed result must match after removing only the
new summary field. No elapsed-clock shifts, source extension, altered reset or
service acceptance, numerical regeneration, or `$finish` timing adjustment is
permitted. The old source97 closure remains byte-identical; new source files
and the exact resolved interpreter are frozen in a new manifest.

Vendor invocation, later synthesis/routing, and source-copy admission remain
parent-owned and require separately reviewed frozen bundle/owner identities.
This preparation is not actual continuous15/native60, causal, lower-clock,
physical, or radio qualification.
